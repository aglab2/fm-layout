<Window x:Class="schedule_fetcher.Views.CreateLayoutView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:schedule_fetcher.Views"
        mc:Ignorable="d"
        xmlns:viewModels="clr-namespace:schedule_fetcher.ViewModels"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:cm="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro.Platform"
        d:DataContext="{Binding Source={d:DesignInstance viewModels:CreateLayoutViewModel}}"
        ui:WindowHelper.UseModernWindowStyle="True" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        Title="Create layout..." Height="750" Width="800">
    <Grid>
        <ui:SimpleStackPanel IsEnabled="{Binding IsNotCreating}" Margin="5" Orientation="Vertical">
            <Button cm:Message.Attach="ReloadTemplates" Content="Reload Layout Templates" HorizontalAlignment="Stretch"/>
            <GroupBox Header="Available layout templates">
                <ListBox Height="600" SelectionMode="Extended" ItemsSource="{Binding Layouts}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="SelectionChanged">
                            <cm:ActionMessage MethodName="ChangeSelectedLayout">
                                <cm:Parameter Value="$eventArgs"/>
                            </cm:ActionMessage>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>
            <TextBlock Text="Shift+Click to select multiple or hold CTRL"/>
            <Button cm:Message.Attach="CreateLayout" IsEnabled="{Binding CanCreate}" HorizontalAlignment="Stretch" Content="Create selected layouts"/>
        </ui:SimpleStackPanel>
        
        <Rectangle Visibility="{Binding IsCreatingVis}">
            <Rectangle.Fill>
                <SolidColorBrush Color="Black" Opacity=".4"/>
            </Rectangle.Fill>
        </Rectangle>
        <Grid Visibility="{Binding IsCreatingVis}">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <ui:ProgressRing Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MaxHeight="100" MaxWidth="100" IsActive="True"/>
            <TextBlock FontSize="24" Grid.Row="1" Text="Creating scenes in OBS. Please wait..." HorizontalAlignment="Center" VerticalAlignment="Top"/>
        </Grid>
        
    </Grid>
</Window>
