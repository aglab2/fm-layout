<UserControl x:Class="schedule_fetcher.Views.LayoutTabsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:schedule_fetcher.Views"
             xmlns:viewModels="clr-namespace:schedule_fetcher.ViewModels"
             d:DataContext="{Binding Source={d:DesignInstance viewModels:LayoutTabsViewModel}}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <TabControl>
        <TabItem Header="Generic Layout">
            <local:LayoutInfoView DataContext="{Binding LayoutInfo}"/>
        </TabItem>
        <TabItem Header="Timer">
            <ScrollViewer>
                <local:TimerView DataContext="{Binding Timer}"/>
            </ScrollViewer>
        </TabItem>
        <TabItem Header="Relay Race">
            <ScrollViewer>
                <local:RelayRaceView DataContext="{Binding RelayRace}"/>
            </ScrollViewer>
        </TabItem>
    </TabControl>
</UserControl>
